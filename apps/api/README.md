# Product API

Planned Python/FastAPI boundary for:

- role- and site-scoped operational/commercial queries;
- typed metric and incident endpoints;
- alert/metric-definition validation;
- audit events;
- read-only AI tools that re-check authorization on every call.

The API will not accept unrestricted analytical SQL from the UI or AI assistant.
