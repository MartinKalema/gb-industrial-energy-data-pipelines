select
    source_schema_id,
    source_schema_version,
    source_system_id,
    synthetic_data,
    delivery_point_assignment_id,
    delivery_point_natural_id,
    delivery_point_name,
    site_natural_id,
    customer_natural_id,
    service_type,
    effective_from_utc,
    effective_to_utc,
    source_revision,
    revision_type,
    assignment_status,
    published_at_utc,
    approval_state,
    approved_at_utc,
    approved_by,
    supersedes_source_revision,
    corrected_at_utc,
    correction_reason_code,
    correction_note,
    pipeline_run_id,
    pipeline_evidence_envelope_id,
    pipeline_ingested_at_utc,
    pipeline_raw_object_uri,
    pipeline_raw_object_sha256,
    pipeline_raw_record_locator,
    pipeline_identity_sha256,
    pipeline_payload_sha256
from {{ source('validated', 'delivery_point_assignment') }}
