select
    source_schema_id,
    source_schema_version,
    source_system_id,
    customer_natural_id,
    customer_version_id,
    legal_name,
    display_name,
    industry_sector_code,
    country_code,
    lifecycle_status,
    tenant_authorization_scope_id,
    effective_from_utc,
    effective_to_utc,
    source_revision,
    revision_type,
    version_record_status,
    published_at_utc,
    approval_state,
    approved_at_utc,
    approved_by,
    supersedes_source_revision,
    corrected_at_utc,
    correction_reason_code,
    correction_note,
    synthetic_data,
    pipeline_run_id,
    pipeline_evidence_envelope_id,
    pipeline_ingested_at_utc,
    pipeline_raw_object_uri,
    pipeline_raw_object_sha256,
    pipeline_raw_record_locator,
    pipeline_identity_sha256,
    pipeline_payload_sha256
from {{ source('validated', 'customer_master') }}
