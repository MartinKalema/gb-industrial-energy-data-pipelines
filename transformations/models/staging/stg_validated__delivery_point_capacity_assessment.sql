select
    source_schema_id,
    source_schema_version,
    source_system_id,
    synthetic_data,
    source_capacity_revision_id,
    delivery_point_natural_id,
    interval_start_utc,
    interval_end_utc,
    assessment_status,
    nameplate_ceiling_mwh_th,
    operational_restriction_mwh_th,
    deliverable_capacity_mwh_th,
    quantity_unit,
    assessment_method,
    assessment_method_version,
    capacity_reason_code,
    source_revision,
    revision_type,
    published_at_utc,
    approval_state,
    approved_at_utc,
    approved_by,
    finalized_at_utc,
    supersedes_source_revision,
    corrected_at_utc,
    correction_reason_code,
    correction_note,
    pipeline_run_id,
    pipeline_evidence_envelope_id,
    pipeline_ingested_at_utc,
    pipeline_raw_object_uri,
    pipeline_raw_object_sha256,
    pipeline_raw_record_locator,
    pipeline_identity_sha256,
    pipeline_payload_sha256
from {{ source('validated', 'delivery_point_capacity_assessment') }}
