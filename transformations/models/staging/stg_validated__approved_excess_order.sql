select
    source_schema_id,
    source_schema_version,
    source_system_id,
    synthetic_data,
    excess_order_natural_id,
    order_interval_line_id,
    delivery_point_natural_id,
    customer_natural_id,
    contract_natural_id,
    interval_start_utc,
    interval_end_utc,
    approved_extra_mwh_th,
    quantity_unit,
    order_state,
    requested_at_utc,
    source_revision,
    revision_type,
    published_at_utc,
    approval_state,
    approved_at_utc,
    approved_by,
    supersedes_source_revision,
    corrected_at_utc,
    correction_reason_code,
    correction_note,
    pipeline_run_id,
    pipeline_evidence_envelope_id,
    pipeline_ingested_at_utc,
    pipeline_raw_object_uri,
    pipeline_raw_object_sha256,
    pipeline_raw_record_locator,
    pipeline_identity_sha256,
    pipeline_payload_sha256
from {{ source('validated', 'approved_excess_order') }}
