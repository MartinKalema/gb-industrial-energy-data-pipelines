select
    source_schema_id,
    source_schema_version,
    source_system_id,
    synthetic_data,
    meter_assignment_id,
    meter_natural_id,
    register_natural_id,
    delivery_point_natural_id,
    assignment_role,
    register_type,
    native_unit,
    calibration_id,
    maximum_plausible_30_min_change,
    effective_from_utc,
    effective_to_utc,
    source_revision,
    revision_type,
    assignment_status,
    published_at_utc,
    approval_state,
    approved_at_utc,
    approved_by,
    supersedes_source_revision,
    corrected_at_utc,
    correction_reason_code,
    correction_note,
    pipeline_run_id,
    pipeline_evidence_envelope_id,
    pipeline_ingested_at_utc,
    pipeline_raw_object_uri,
    pipeline_raw_object_sha256,
    pipeline_raw_record_locator,
    pipeline_identity_sha256,
    pipeline_payload_sha256
from {{ source('validated', 'revenue_meter_assignment') }}
